.DS_Store
node_modules/
dist/
out/
*.log
.vscode/
.idea/

# generated planner PDFs are large (~17MB each) and fully reproducible
# via `npm run build`; the hyperlinked source HTML is committed instead.
# (guide PDFs are small and are committed.)
export/*/goyo-najeon.pdf
export/*/goyo-light.pdf
export/*/goyo-hanji.pdf

# INAE Practice PDF: 1.6MB, regenerated byte-for-byte by `npm run drawingbook`
# + `npm run pdf`. Same rule as the GOYO planner PDFs — the hyperlinked source
# HTML is committed instead, since that is what is actually reviewable.
export/inae/inae-practice.pdf
